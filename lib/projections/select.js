/**
 * Module dependencies
 */

var _ = require('lodash')
  , util = require('util');


/**
 * Project `tuples` on `fields`.
 * 
 * @param  { Object[] }  tuples    [i.e. filteredData]
 * @param  { String[] }  fields    [i.e. schema]
 * @return { Object[] }
 */
module.exports = function (tuples, fields) {
  return tuples;
};
